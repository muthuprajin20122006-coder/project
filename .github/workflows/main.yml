name: Basic CI workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout reposirory
      - uses: actions/checkout@v4
        run: echo "Hello GitHub Actions workflows in running successfully!"

